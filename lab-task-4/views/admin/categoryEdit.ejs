<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Product Category</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h1>Edit Product Category</h1>
      <form
        action="/admin/category/editForm/<%= category._id %>"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="mb-3">
          <label for="categoryName" class="form-label">Category Name</label>
          <input
            type="text"
            class="form-control"
            id="categoryName"
            name="categoryName"
            value="<%= category.categoryName %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="image" class="form-label">Upload New Image (optional)</label>
          <input type="file" class="form-control" id="image" name="categoryPicture" />
          <small class="form-text text-muted">Leave blank to keep the current image.</small>
        </div>
        <div class="mb-3">
          <label for="type" class="form-label">Category Type</label>
          <select class="form-select" id="type" name="type" required>
            <option value="Bedroom" <%= category.type === "Bedroom" ? "selected" : "" %>>Bedroom</option>
            <option value="Living Room" <%= category.type === "Living Room" ? "selected" : "" %>>
              Living Room
            </option>
            <option value="Home Office" <%= category.type === "Home Office" ? "selected" : "" %>>
              Home Office
            </option>
            <option value="Dining Room" <%= category.type === "Dining Room" ? "selected" : "" %>>
              Dining Room
            </option>
            <option value="Home" <%= category.type === "Home" ? "selected" : "" %>>Home</option>
          </select>
        </div>
        <div class="mb-3">
          <label for="linkName" class="form-label">Link Name</label>
          <input
            type="text"
            class="form-control"
            id="linkName"
            name="linkName"
            value="<%= category.linkName %>"
            required
          />
        </div>
        <button type="submit" class="btn btn-primary">Save Changes</button>
        <a href="/admin/categories" class="btn btn-secondary">Cancel</a>
      </form>
    </div>
  </body>
</html>
